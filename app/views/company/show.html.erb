<div class="row">
	<div class="col-md-12">
		<% flash.each do |message_type, message| %>
			<div class="alert alert-<%= message_type %>"><%= message %></div>
		<% end %>
		Your company has been created.  Default admin also been created. 
		Credentials of default user are mailed at <%= "admin@#{@company.name}.com" %>
		Thank You!
	</div>
</div>