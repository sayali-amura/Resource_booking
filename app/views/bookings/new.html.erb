<h2>Booking Request</h2>
<div class="row">
<div class="col-md-6 col-md-offset-3">

<%= render "error" %>
	<%= form_for :booking ,url: bookings_path do |f|%>

		<%= f.select :resource_id , id: "resource_id"  do  %>
					<% resource_array.each do |c| %>
						
						<%= content_tag(:option,c.first,value: c.last, class: 'form-control') %>
						
						<br>
					<%end %>
		<%end %>
			Booking Subject
		<%= f.text_field :comment, placeholder: "Purpose of booking", class: 'form-control' %>
			Date
		<%= f.date_field :date_of_booking %>
			Time Slot
		<%= f.select :slot do  %>
			
			<div id="timeslot_options">
				
			</div>	
					
		<%end %>

		Priority

		<%= f.select :priority  do %>
			<% [['Low',0],['Medium',1],['High',2]].each do |c| %>
				<%= content_tag(:option,c.first,value: c.last, class: 'form-control') %>
			<%end %>
		<%end %>
		<%= f.submit "Book", class: "btn btn-primary" %>
	<%end %>
</div>
</div>
<div id="check">
	
</div>